func before() {
    print("before()")
}

func after() {
    print("after()")
}

func run() {    
    resp := load.http({
        url: "https://github.com",
        method: "GET",
        headers: {
        },
    })
    if resp != nil {
        if resp.statusCode == 200 {
            resp.success()
        } else {
            resp.fail("status is not 200 {resp.statusCode}")
        }
    }    
}