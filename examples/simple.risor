func run() {    
    resp := load.http({
        url: "https://google.com",
        method: "GET",
        headers: {
        },
    })
    if resp != nil {
        if resp.statusCode == 200 {
            resp.success()
        } else {
            resp.fail('status 200 != {resp.statusCode}')
        }
    }
}
